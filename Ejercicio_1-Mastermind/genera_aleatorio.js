/**
 * Genera un numero aleatorio del 0 al 9
 * @returns {number} - numero
 */
function genera_aleatorio() {
    /**
     * numero aleatorio
     * @type {number}
     */
    var numero = Math.floor(Math.random() * (10 - 0)) + 0
    return numero

}